{% extends 'base.html.twig' %}
{% block title %}Réinitialisation{% endblock %}
{% block content %}
    <div class="d-flex justify-content-center limiter" style="margin-top: 170px; margin-bottom: 70px">
        <div class="register-container">
            <div class="request-wrap">
                <span class="register-form-title">Réinitialisation</span>
                {{ form_start(form, { 'action': path('app_user_reset', {'token': token}), 'attr': {'class': 'register-form'} }) }}

                <div class="form-group my-2 {% if not form.vars.valid %} has-error{% endif %}">
                    {{ form_row(form.password.first) }}
                </div>

                <div class="form-group my-2 {% if not form.vars.valid %} has-error{% endif %}">
                    {{ form_row(form.password.second )}}
                </div>

                <button class="register-form-btn btn btn-paletteEvent btn-success" type="submit">
                    Valider
                </button>
                {{ form_end(form) }}

            </div>
        </div>
    </div>
    
{% endblock %}
