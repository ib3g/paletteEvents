<table class="body-wrap">
    <tr>
        <td></td>
        <td class="container" width="600">
            <div class="content">
                <table class="main" width="100%" cellpadding="0" cellspacing="0">
                    <tr>
                        <td class="content-wrap aligncenter">
                            <table width="100%" cellpadding="0" cellspacing="0">
                                <tr>
                                    <td>
                                        <div class="text-center"
                                             style="font-weight: bold; display: flex; align-items: center;">
                                            <img style="width: 50px;height: 50px"
                                                 src="{{ absolute_url(asset('build/images/logo.png')) }}"
                                                 alt="logo not found">
                                            PaletteEvents
                                        </div>

                                        <p>Bonjour {{ event.owner.fullName }},</p>
                                        <p>
                                            {% if event.status == 'new' %}
                                                Nous avons le plaisir de vous informer que votre demande de création
                                                d'événement (<strong>{{ event.title }}</strong>) a été approuvée,
                                                et un email d'invitation a été envoyé à tous les animateurs de l'événement.
                                                <br>
                                                <br>
                                                L'événement a été publié voici le lien vers l'événement :
                                                <a target="_blank" href="{{ url }}">{{ event.title }}</a>
                                            {% else %}
                                                Nous regrettons de vous informer que votre demande de création d'événement
                                                (<strong>{{ event.title }}</strong>) n'a pas été approuvée.
                                                Pour de plus amples informations ou pour obtenir des détails sur les
                                                raisons de cette décision, nous vous invitons à contacter notre service
                                                administratif.
                                                <br>
                                            {% endif %}
                                        </p>
                                    </td>
                                </tr>
                            </table>
                        </td>
                    </tr>
                </table>
            </div>
        </td>
        <td></td>
    </tr>
</table>

