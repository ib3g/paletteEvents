{{ form_start(form) }}
    {{ form_widget(form) }}

    <a class="btn btn-chellal btn-secondary" href="{{ path('app_prix_index', {'id': event.id }) }}">Retour à la liste</a>
    <button type="submit" class="btn btn-chellal btn-paletteEvent">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
